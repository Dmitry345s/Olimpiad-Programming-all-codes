#include<iostream>
#include<vector>

using namespace std;

struct point{
    double x, y;
};

struct vct{
    double x, y;
};

int main() {
    double s;
    cin >> s;
    point a, b;
    cin >> a.x >> a.y >> b.x >> b.y;
    return 0;
}
